# inventory.yml

all:
  vars:
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    ansible_user: anton
  children:
    proxmox:
      hosts:
        proxmox-server.local:
          ansible_host: proxmox-server.local
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/torre-linux
    docker_servers:
      hosts:
        plex-server.local:
        wireguard-server.local:
        samba-server.local:
        linuxworkstation-server.local:
    file_browser_servers:
      hosts:
        wireguard-server.local:
        linuxworkstation-server.local:
    plex_server:
      hosts:
        plex-server.local:
    wireguard_server:
      hosts:
        wireguard-server.local:
    samba_server:
      hosts:
        samba-server.local:
    linux_workstation_server:
      hosts:
        linuxworkstation-server.local:
